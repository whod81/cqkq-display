Each directory should correlate to a team name in the challonge.  It is case sensitive.  It will strip out anything that is after a ' (' set of characters so you can put scene homes in the challonge but not in the directory.  

Each team directory should have JPG or PNG files that are the person's name.  It will strip off the extension and use that as their name.
